import { UseLocalStorageDemo } from "../hooks/useLocalStorage/useLocalStorage.demo";

export const DocumentPage = () => {
   return (
      <div>
         <UseLocalStorageDemo />
      </div>
   );
};
